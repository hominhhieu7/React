import { LEADERS } from '../../shared/Leaders';



// const initialState = {
//     Leaders: LEADERS
// }
export const Leaders = (state = LEADERS, action) =>{
    switch(action.type){
        default: 
        return state;
    }
}